# Taxes App Family

Current tax workflow apps live under this folder:

- `api/`: Fastify backend for local tax workflows
- `web/`: React frontend for document review and remediation

Keep shared contracts in `packages/shared` and add future product families alongside `taxes/`, not inside it.
