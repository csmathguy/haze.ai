# Plan Web Instructions

- Optimize for fast backlog review, clear status, and visible acceptance criteria.
- Use theme tokens from `apps/plan/web/src/theme/` instead of ad hoc color literals.
- Keep planning logic on the API side; the frontend should render typed planning contracts and submit typed updates.
- Make task, criterion, status, and audit-reference state easy to scan before adding decorative complexity.
