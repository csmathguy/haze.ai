# Handoff Checklist

- confirm all changes stayed inside the allowed scope
- summarize any interface or contract changes
- note whether downstream slices need a rebase or merge
- list the validation commands that passed
- call out any remaining conflict hotspot before handoff
- confirm the slice is ready for PR handoff and human review, not direct agent merge
